spring:
  application:
    name: client-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          search-paths: respo
          default-label: master
          username:
          password:


server:
  port: 8100

